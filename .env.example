# ============================================
# CloudTrail Security Bot 환경 변수 설정
# ============================================
# 이 파일을 복사하여 .env 파일을 생성하세요:
# cp .env.example .env

# --------------------------------------------
# Slack Configuration (필수)
# --------------------------------------------
# Bot User OAuth Token - OAuth & Permissions에서 확인
# https://api.slack.com/apps -> Your App -> OAuth & Permissions
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# App-Level Token - Socket Mode 활성화 시 생성됨
# https://api.slack.com/apps -> Your App -> Basic Information -> App-Level Tokens
SLACK_APP_TOKEN=xapp-your-app-token-here

# --------------------------------------------
# AWS Configuration (필수)
# --------------------------------------------
# 환경 타입 (dev, stg, prd)
# DB 연결 및 SSM Parameter Store 경로에 사용됨
ENV_TYPE=dev

# 기본 AWS 리전
AWS_REGION=ap-northeast-2

# AWS 프로파일 (선택사항 - 로컬 개발 시 사용)
# AWS_PROFILE=your-profile-name

# --------------------------------------------
# Bedrock Configuration (선택사항)
# --------------------------------------------
# Bedrock 모델 ID (기본값: Claude 3.5 Sonnet)
# 사용 가능한 모델:
# - us.anthropic.claude-3-5-sonnet-20241022-v2:0 (권장)
# - us.anthropic.claude-3-haiku-20240307-v1:0 (빠름, 저렴)
# - us.anthropic.claude-3-opus-20240229-v1:0 (고성능)
BEDROCK_MODEL_ID=us.anthropic.claude-3-5-sonnet-20241022-v2:0

# --------------------------------------------
# AgentCore Configuration (운영 환경)
# --------------------------------------------
# AgentCore 원격 호출 모드 사용 여부
# true: Slack Bot이 AgentCore API를 호출
# false: Slack Bot 내에서 직접 Agent 실행 (기본값)
USE_AGENTCORE_REMOTE=false

# AgentCore Runtime Endpoint URL
# agentcore deploy 후 출력되는 URL 입력
# AGENTCORE_ENDPOINT=https://your-agentcore-endpoint.amazonaws.com

# AgentCore 내부 실행 모드 (AgentCore 배포 시에만 사용)
# USE_AGENTCORE=true

# --------------------------------------------
# Logging Configuration (선택사항)
# --------------------------------------------
# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# --------------------------------------------
# Database Configuration
# --------------------------------------------
# 환경변수가 설정되면 해당 값을 사용하고,
# 비어있으면 SSM Parameter Store에서 자동으로 로드합니다.
# (/fitcloud/{ENV_TYPE}/db/...)

# DB_HOST=your-db-host.rds.amazonaws.com
# DB_PORT=3306
# DB_USER=admin
# DB_PASSWORD=your-db-password
# DB_NAME=fitcloud
# DB_SECRET_TITLE=your-secret-title-for-encryption
